---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html>
	<head>
		<title>{title}</title>
		<meta charset="UTF-8" />
	</head>
	<body>
		<div class="w-screen h-screen flex flex-col items-center font-mono gap-8">
			<slot />
		</div>
	</body>
</html>
